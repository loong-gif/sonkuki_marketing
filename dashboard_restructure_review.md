# 看板重构提案审阅意见

> 审阅对象：针对当前 SONKUKI Growth Intelligence 看板的五部分重构问题清单
> 审阅日期：2026-08-13
> 数据基准：`outputs/sonkuki_gsc_insights_site/public/snapshot.json`（生成于 2026-08-13T14:51Z，GSC 2026-04-22 → 2026-08-08）
> 代码基准：`outputs/sonkuki_gsc_insights_site/public/index.html`（静态导出版渲染层，与 `worker/index.js` 同源）
> 交付物：本文档仅审阅，未改动任何看板代码或数据。

---

## 1. 执行摘要

**结论：提案方向正确、可以采纳，落地成本可控，数据层零改动。**

- 提案引用的事实与数字**基本全部属实**——经用 `snapshot.json` 复刻渲染逻辑逐一核对，8 项关键数据引用中 7 项一致、1 项需精化（见第 2 节核对表）。
- 提案准确抓住了一个**真实 bug**：市场对标页「价格差」列整列渲染为字面量 `undefined`（`public/index.html` 第 90 行，`priceDiff(u)` 已定义但从未应用到行数据）。这是当前唯一被提案点出、且应立即修复的代码缺陷。
- 有 **1 处结论性表述与事实相反**（「整体定价略高于中位数」，实际 8 组中 6 组低于竞品中位价）与 **5 处需要精化的表述**（见第 3 节），均不影响提案骨架，改措辞即可。
- 提案的四个分区、三段式文案、三层 Benchmark、「可以放心/不要用」方法论等设计，与现有渲染代码的模式（`card / todo / collapse / caveat / 锚点跳转 / tag-warn`）契合度极高，大部分是**文案与布局重组**，不需要新增数据字段。
- 你已确认后续落地采用「按提案改 4 区」的导航结构，第 7 节给出分步实施顺序（本期不实施）。

---

## 2. 事实核对表

验证方法：用 `jq` 复刻 `public/index.html` 中的渲染逻辑（`queryRows`、`renderOpportunity`、`renderProduct`、`renderBenchmark` 等），对 `public/snapshot.json` 重新计算并与看板展示值比对。

| # | 提案引用 | 核实值 | 结果 |
|---|---------|--------|------|
| 1 | 非品牌曝光 58.1% | 16,841 / 28,971 = 58.1%（品牌 41.9%） | ✅ 一致 |
| 2 | 主页主 URL 占比 13.7%（sonkuki 品牌词） | `sonkuki`：曝光 4,174、点击 216、主页占比 573/4,174 = **13.7%**、12 个有效竞争页、风险 HIGH | ✅ 一致 |
| 3 | 可优化关键词 10 个（Rank 4–20、高曝光） | VALID 查询 Top 20 曝光 ∩ 排名 4–20 = **10 个** | ✅ 一致 |
| 4 | Potential clicks 66（4% 基准） | Σ `曝光 × max(0, 4% − CTR)` = **65.96 ≈ 66** | ✅ 一致 |
| 5 | 高/中内耗 180（其中 HIGH 119） | 1,838 个 VALID 查询：**119 HIGH / 61 MEDIUM / 1,658 LOW** | ✅ 一致 |
| 6 | 修正 4 个 Top 10 但 CTR<2% 的 Pergola 页、曝光 2,300+ | 看板「Top 10 低 CTR」显示的前 4 个 SKU 全部是 Pergola，合计 **2,362 曝光**（768+571+553+470） | ✅ 一致（但见修正②） |
| 7 | 2 个无评论的 Pergola 尺寸，竞品中位 50 条 vs 0 条 | 9×14 ft Steel（自家 0 / 竞品中位 50）、10×10 ft Steel（自家 0 / 竞品中位 **5**） | ⚠️ 部分（见修正③） |
| 8 | 10×13 ft Steel：1,394 vs 1,372、8 vs 50、5.0 vs 4.88 | 价格中位 1,394 vs 竞品 1,372；评论 8 vs 50；评分 5.0 vs 4.88 | ✅ 全部一致 |
| 9 | Benchmark 表存在 `undefined` | 属实：`_pd` 列渲染 `r._pdTone`/`r._pdTxt`，行数据上不存在这两个键（`index.html:90`） | ✅ bug 确认 |

补充核实的背景事实（供文档引用）：

- 整体 CTR 1.92%（556 点击 / 28,971 曝光）。
- 内耗 HIGH 的 Top 词**几乎全是品牌词**：`sonkuki`(4,174)、`sonkuki pergola`(1,841)、`sonkuki umbrella`(1,680)、`sonkuki patio louvered pergola…`(1,316)、`sonkuki pergola screen`(333)…；前 15 个 HIGH 内耗词中仅 2–3 个非品牌词。
- Rank 4–20 机会集的 10 个词中 **6 个是品牌词**；非品牌只有 4 个：`are led umbrellas worth it`(395)、`pergola meaning`(236)、`best patio umbrella for windy areas`(177)、`best thinnest patio furniture`(173)，CTR 全部 0–0.6%。
- 相关性过滤：2,013 个原始 query → 1,838 VALID（91.3%）/ 7 IRRELEVANT / 168 UNKNOWN，与提案「91%」一致。
- 快照质量检查共 19 项，当前整体状态 `warning`（2 项 warning：竞品 listing 去重 415→378、销量为聚合估算）。

---

## 3. 需修正的 6 处表述

以下修正只影响措辞，不影响提案结构：

### ① 「品牌曝光占比 X%」→ 41.9%，且补更强的证据
- 实际：品牌曝光 12,130 / 28,971 = **41.9%**（非品牌 58.1%）。
- 更强的证据链不是占比本身，而是：**HIGH 内耗 Top 词几乎全是品牌词**（见第 2 节补充），以及机会集 10 词中 6 个是品牌词。建议「统一承接 sonkuki 品牌词」的文案写成：`sonkuki 词簇 4,174 次曝光分散在 12 个页面、主页仅承接 13.7%，且为 HIGH 内耗——收口到 1 个主页后，品牌点击还有明显提升空间`。

### ② 「4 个 Pergola 商品页、2,300+ 曝光」→ 实际阈值内 11 个 SKU / 3,834 曝光
- 阈值（曝光 ≥100 · 排名 ≤10 · CTR <2%）下有 **11 个 SKU、合计 3,834 曝光**（8 个 Pergola / 3 个伞），看板的「Top 10 低 CTR」面板只 `slice(0,4)` 展示了曝光最高的前 4 个（2,362）。
- 建议：行动清单要么按 8 个 Pergola 全部列出（3,307 曝光），要么保留前 4 并明确标注「另有 7 个同类 SKU 未展示」；同时「Top 10 低 CTR」的指标名与展示数（10 vs 4）不一致，也应统一口径。

### ③ 「竞品评论中位 50 条 vs 自家 0 条」→ 只对 9×14 Steel 成立
- 9×14 ft Steel：自家 0 条 vs 竞品单位中位 **50** 条 ✅
- 10×10 ft Steel：自家 0 条 vs 竞品单位中位 **5** 条（不是 50）
- 建议证据文案拆成两行分别引用，避免被质疑「数字对不上」。

### ④ 「整体定价略高于中位数」→ 与事实相反，建议改写
- 8 个产品组中 **6 组低于竞品中位价**：12×14 Steel(−13%)、10×13 Alu(−12%)、10×10 Alu(−20%)、12×20 Steel(−47%)、12×20 Alu(−36%)、10×10 Steel(−39%)；仅 2 组略高：10×13 Steel(+1.6%)、9×14 Steel(+15.7%)。
- 建议顶层一句话改为：**「在 Pergola 品类，我们多数产品组的价格低于竞品中位（有价格优势），但社会证明（评论量）普遍落后，评分在部分组也有差距。」**——这才与数据一致，也和「建评论」的行动建议自洽（价格已低、无需靠降价竞争）。

### ⑤ 「预估可新增约 XX 点击」（品牌统一）→ 当前无此模型
- 现有模型只有 Rank 4–20 的 `potential = 曝光 × max(0, 4% − CTR)`（合计 66），**没有**品牌词簇的统一承接收益模型。
- 建议二选一：a) 不写具体数字，用「收口到 1 个主页」的方向性表述；b) 给品牌词簇补一个独立的方向性估算（如按主页份额提升带来的曝光再分配粗算），并明确标注「仅排序参考、不可与其他模块累加」。

### ⑥ 「91% 搜索词」→ 精确为 91.3%（1,838/2,013），措辞可保留

---

## 4. 逐节审阅

### 4.1 第一节：顶层结构（3 主区 + 1 方法区）— **采纳**

方向正确。当前 6 个 tab（Executive / Opportunity / Product SEO / Marketplace / Competitors / Benchmark）各自为政，提案把它们拧成「看规模 → 定动作 → 对标市场 → 知道边界」的完整叙事，是合理的。

- **首页首屏行动摘要**：现有 Executive 页已经是「4 指标卡 + 本期决策提示卡 + 趋势/品牌结构/排名区间图 + 快照质量」，提案要求首屏只放 3–5 个带证据的动作、图表与表格下沉——与现有 `card--primary` + 锚点（`#exec-action`）模式完全兼容，动作证据数字全部可现算（第 2 节核对表）。
- **「从词到页」四步**：Opportunity 页已具备机会矩阵、优先清单、内耗表、执行规则四要素，提案要求按「看盘子 → 找词 → 收口 → 改页」重排顺序并补一句翻译文案，属于页内重排 + 文案，无新计算。
- **市场对标定位为「月度/季度复盘」**：合理，且能解释为什么 Benchmark 数据不需要每天刷。
- **折叠/tooltip 提供细节**：Benchmark 商品表的 SKU 行已经是 `collapse` 展开式（`skuDetail`），提案的「折叠全表」可直接复用该组件。

### 4.2 第二节：各模块文案与呈现 — **采纳（3 处注意点）**

1. **三问三答（Decision cues）**：现有 3 条 cue 是「非品牌覆盖 / 效率优先 / 数据边界」，提案改成「扩盘还是提效 / 最大效率损失 / 站内SEO还是市场验证」。数据支撑齐备：
   - Q1：非品牌占比 58.1% + 按日点击趋势（Executive 已有 `byDay` 图）→ 可直接回答；
   - Q2：用 11 个低 CTR + 5 个零点击 SKU 的曝光量给出「损失点击区间」——按看板自己的 potential 公式粗算，前 4 个约 78 次、全部 11 个约 135 次（4% 基准，方向性）；
   - Q3：SEO×market proof 散点在 Marketplace 页已有（`renderMarketplace` 的 `matched` 样本），Benchmark 异常点可引用 12×20 Steel 价差 −47% 等。**注意**：Q3 的答案要依赖「本期筛选范围」，建议给一个默认口径（如全期 Pergola）避免被筛选改变结论。
2. **机会矩阵视觉编码（右上角标「先看」、颜色区分风险）**：现有散点图只画位置×曝光、无风险编码；增加形状/颜色按 Risk 区分是纯前端改动，可行。但注意机会集本身只有 10 个点、且 6 个是品牌词——「先看右上角」的实际价值要配合第 3 节修正⑤一起设计，避免看起来「高曝光机会」全是品牌词。
3. **合并「问题类型」列 + 行动顺序**：重要发现——低 CTR 与零点击列表**有重叠**：`SML-BP-DD1220`(358 曝光、排名 1.1)、`SML-BP-3030`(342、排名 1.9) 同时命中「排名好但 CTR 低」和「排名很好但 0 点击」。合并列表必须定义去重规则，建议优先序：**排名 1–3 且 0 点击（先看主图/标题/Schema）→ 排名 4–10 且 CTR 低（再看内容与内链）**，与提案建议一致。

### 4.3 第三节：市场对标三层结构 — **采纳（顶层一句话按修正④改写）**

- 三层（一句话结论 → 3–5 组卡片 → 折叠全表）可行：现有 `priorityActions`（5 条，含问题/证据/建议）正好可以作为卡片层的内容源；全表已有、加 `collapse` 包裹即可。
- **必须一并修复 `undefined` bug**：折叠后这个 bug 会从「每行都可见」变成「点开才见」，更容易被漏掉，应在本次重构中先修。
- 「低置信度」角标：`matchQuality` 字段已存在于每个 unit（8 组中 2 组低置信度、1 组中置信度），角标可直接渲染，无需新数据。

### 4.4 第四节：方法论与信任区 — **采纳**

- 现有「快照质量」19 项检查在 Executive 页底部（`qualityHtml`，span-12），搬进第 4 区是纯位置迁移。
- 「可以放心用 / 暂不要用」的文案框架优于现在的一长串技术 bullet，建议把现有散落在各卡片的 caveat（「Potential clicks 仅作排序参考」「销量为模型估算」「散点仅用于机会探索」「仅统计 VALID 查询」）在方法区统一收口成「使用边界」清单，卡片上保留一行简短提醒即可。
- 注意措辞一致性：提案写「评论做了去重和去身份化处理」——快照的 19 项检查里确有「Reviews deduplicated by review UID」「Snapshot excludes PII」两条背书，可以放心写。

### 4.5 第五节：风格 — **采纳**

- 三段式「问题 → 结论 → 证据」与现有中文为主、英文缩写注释的双语风格一致，改动为纯文案。
- 「样本不足」灰化 + 「低置信度」角标：当前只有文字 caveat（如 VOC 卡片「样本不足，暂不判断」）与 `tag-warn` 低置信度标签；改成卡片级视觉状态（灰化/角标）是 CSS 级改动，成本低，且能避免「文字 caveat 被淹没」。

---

## 5. 结构性发现（提案之外的 3 个点）

1. **HIGH 内耗几乎全是品牌词** —— 「统一承接品牌词」不只是一个行动，而是当前内耗问题的**主体**（119 个 HIGH 里按曝光排序前 15 名几乎全是 `sonkuki*` 词）。这正好支持提案把品牌收口列为行动 1，也说明内耗视图的默认排序应从「曝光」改为「风险 × 曝光」，让品牌词簇在表头就可见。
2. **机会集被品牌词主导** —— 非品牌「高曝光内容词」实际只有 4 个（曝光 173–395），提案行动 4「优化 3–5 个高曝光非品牌关键词」数字上成立，但要注意这 4 个词多为信息型查询（`pergola meaning`、`are led umbrellas worth it`），内容优化的承载页是博客/FAQ 而非商品页，行动描述建议点明「内容结构」而非「商品页」。
3. **零点击与低 CTR 重叠** —— 直接影响「问题类型」列的去重设计（见 4.2-3）。

---

## 6. 可行性评估

| 维度 | 评估 |
|------|------|
| 数据层 | 零改动。提案所需全部数字（第 2 节核对表）都可从现有 `snapshot.json` 现算 |
| 渲染层 | 三份拷贝需同步：`public/index.html`（静态导出）、`worker/index.js`、`dist/server/index.js`；`public/index.html` 与 `worker/index.js` 同源，改完需重新走构建 |
| 构建/验证 | 已有完整流程：`npm run build && npm run validate && npm test`（`scripts/build.sh`），测试覆盖看板指标、内耗、benchmark 三个测试文件 |
| 风险 | 改动集中在单一渲染文件；`undefined` 修复、文案重组风险极低；导航 4 区重组的风险集中在 URL `?page=` 参数与锚点兼容，需保留旧锚点或做重定向映射 |
| 部署 | Vercel（静态导出）+ Sites，部署回滚路径清晰 |

建议落地顺序（第 7 节）中，步骤①（undefined 修复）与步骤⑥（文案统一）可随时独立上线，不需要等大重构。

---

## 7. 落地建议（按你选定的「4 区」方案，本期不实施）

### 7.1 六 tab → 四区迁移映射

| 现有 | 新 4 区 |
|------|---------|
| 总览 Executive | **① 本期行动摘要**（首屏：3–5 个带证据的动作卡 + 三问三答决策提示；趋势/品牌结构/排名区间图表下沉到 ②） |
| 关键词机会 Opportunity | **② 自然搜索 & 站内 SEO**（机会矩阵 + 优先清单 + 内耗 + 执行规则，按「从词到页」重排；清单增加内耗风险列） |
| 页面与商品 Product SEO | **② 自然搜索 & 站内 SEO**（SEO 行动面板合并为「问题类型」单列表 + 着陆页 + 页面类型 + 商品明细 + 渠道价格对比） |
| 市场验证 Marketplace | **③ 市场验证 & 竞品对标**（评分×评论、低星主题、SEO×market proof） |
| 竞争者分析 Competitors | **③ 市场验证 & 竞品对标**（品牌社会证明、价格×proof、价格带、热度、盯防清单） |
| 市场对标 Benchmark | **③ 市场验证 & 竞品对标**（一句话结论 → 3–5 组卡片 → 折叠全表 + VOC/高于优于市场） |
| （Executive 底部）快照质量 | **④ 数据与方法**（19 项质量检查 + 相关性过滤明细 + 「可以放心用/暂不要用」使用边界） |

### 7.2 分步实施顺序

1. **修复 `undefined`（零风险，可先上线）**：`renderBenchmark` 中把 `priceDiff(u)` 的结果注入每个 unit（`_pdTxt`/`_pdTone`），或把 `_pd` 列渲染改为函数内联计算；同步三份渲染拷贝；跑 `npm run validate && npm test`。
2. **首页行动摘要卡片**：Executive 顶部新增「本期行动摘要」`card--primary`，5 条动作 + 证据（数字全部来自第 2 节核对表），每条挂 `去详情` 锚点（复用现有 `#opp-action`、`#product-action`、`#benchmark-action` 模式）；决策提示改为三问三答。
3. **4 区导航重组**：nav 6 tab → 4 区；`render()` dispatch 与 `state.page`/`?page=` 参数映射（旧 URL 参数做兼容映射）；把 Executive 的品牌结构/排名区间/点击趋势卡片迁入 ②。
4. **搜索&SEO 区「从词到页」重排**：Opportunity 页内按四步顺序重排；优先清单增加「内耗风险」列；SEO 行动面板合并为单列表 + 「问题类型」列，按「排名 1–3 零点击 → 排名 4–10 低 CTR」去重排序（注意 DD1220/3030 双命中）。
5. **市场对标三层化**：一句话结论卡（按修正④措辞）+ 3–5 组产品卡片（取 `priorityActions`/按价格差与评论差排序）+ 折叠全表（修好 `undefined` 后包裹 `collapse`）；低置信度角标直接渲染 `matchQuality`。
6. **方法论区与文案统一**：新建 ④ tab，搬入 19 项质量检查 + `queryRelevance` 明细 + 「可以放心/暂不要用」清单；全站文案按「问题→结论→证据」与修正①–⑥统一；「样本不足」灰化与「低置信度」角标的 CSS 落地。

---

## 附录：验证方法与证据位置

- 数字验证：`jq` 复刻 `public/index.html` 渲染逻辑（`queryRows`、`renderOpportunity`、`renderProduct`、`renderBenchmark`）对 `public/snapshot.json` 重算，与看板展示值比对一致。
- 代码位置：渲染层 `outputs/sonkuki_gsc_insights_site/public/index.html`（`renderExecutive` L83、`renderOpportunity` L85、`actionPanelBody`/`renderProduct` L86–87、`renderMarketplace` L88、`renderCompetitor` L89、`renderBenchmark` 及 `undefined` L90、`qualityHtml` L79、导航/分页 L24–32）；同源于 `worker/index.js`。
- 快照质量：`snapshot.json.quality`（19 项检查）、`snapshot.json.queryRelevance`（2013→1838 VALID）。
- 构建流程：`SONKUKI_HANDOFF.md` L30–45（refresh → snapshot build → `npm run build/validate/test` → 部署）。
